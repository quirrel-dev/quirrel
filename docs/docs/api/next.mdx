import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Next.js

<Tabs>
<TabItem value="app" label="App Router">

#### Deceleration

```typescript title="app/api/queues/sample/route.ts"
import { Queue } from "quirrel/next13";

export const { POST, queue: sampleQueue } = Queue(
  "api/queues/sample",
  async (job, meta) => {
    // do something
  }
);
```

Creates a new Queue.
Make sure to export it from an [API Route](https://nextjs.org/docs/app/building-your-application/routing/router-handlers), otherwise it won't work.

#### Usage

```typescript
import { sampleQueue } from "./app/api/queues/sample/route.ts";

sampleQueue.enqueue();
```

Once you import the queue instance, you can add a new job to it with `enqueue`.

#### Parameters

```ts
function Queue<T>(
  path: string,
  worker: (job: T, meta: JobMeta): Promise<void>,
  defaultJobOptions?: { exclusive?: boolean }
): { POST: (req: Request) => Promise<Response>, queue: QueueInstance<T> }
```

</TabItem>
<TabItem value="page" label="Pages Router">

#### Deceleration

```typescript title="pages/api/queues/sample.ts"
import { Queue } from "quirrel/next";

export default Queue("api/queues/sample", async (job, meta) => {
  // do something
});
```

Creates a new Queue.
Make sure to export it from an [API Route](https://nextjs.org/docs/api-routes/introduction), otherwise it won't work.

#### Usage

```typescript
import { sampleQueue } from "./app/api/queues/sample.ts";

sampleQueue.enqueue();
```

Once you import the queue instance, you can add a new job to it with `enqueue`.

#### Parameters

```ts
function Queue<T>(
  path: string,
  worker: (job: T, meta: JobMeta): Promise<void>,
  defaultJobOptions?: { exclusive?: boolean }
): QueueInstance<T>
```

</TabItem>
</Tabs>

| Parameter           | Usage                                                           |
| ------------------- | --------------------------------------------------------------- |
| `path`              | The route that this queue is reachable at.                      |
| `worker`            | a function that takes the job's payload and returns a `Promise` |
| `defaultJobOptions` | Optional. Use to set default options applied to every job.      |
