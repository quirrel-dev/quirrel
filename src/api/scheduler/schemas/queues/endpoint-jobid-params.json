{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "/queues/:endpoint/:id params",
  "type": "object",
  "properties": {
    "endpoint": {
      "type": "string",
      "description": "The endpoint that jobs are sent to."
    },
    "id": {
      "type": "string",
      "description": "The job's ID"
    }
  },
  "required": ["endpoint", "id"],
  "additionalProperties": false
}
